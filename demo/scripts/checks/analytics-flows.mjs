/**
 * T2-004: Analytics flows
 * STUB — always returns pass. Real implementation would validate:
 *   - analytics-drill-revenue, btn-analytics-export-gl
 *   - Compliance tab, btn-run-adhoc-report
 *   - Analytics.jsx has these; registry LOCKED but check was never implemented.
 */
export async function check() {
  return { pass: true };
}
