/**
 * T2-007: CRM Change Propagation to Billing + Finance
 * STUB — always returns pass. NORTH_STAR: after Apply Credit / Resolve Case, show propagation
 * block. data-demo="crm-billing-link". Emberlyn prompt: "Show me what changed in Finance."
 */
export async function check() {
  return { pass: true };
}
